import { FC } from 'react'
import { ExploreButton } from '../components/ExploreButton'

export const Provider: FC = () => {
  return (
    <div id="view-provider">
      this is the provider view
      <ExploreButton />
    </div>
  )
}
